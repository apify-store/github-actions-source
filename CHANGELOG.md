# Changelog

## 202-05-17

- Enforce CHANGELOG.md updates on PRs in the `pr-build-test` reusable workflow. PRs must modify `./CHANGELOG.md` unless the title starts with `chore`, `refactor`, `test`, `ci`, `build`, or `docs`, or the PR carries the `internal` label.
